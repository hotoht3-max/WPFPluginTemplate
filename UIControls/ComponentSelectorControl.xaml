<UserControl x:Class="Apibim.Plugins.BuiltUpColumn.UIControls.ComponentSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UIControls="clr-namespace:Tekla.Structures.Dialog.UIControls;assembly=Tekla.Structures.Dialog"
             mc:Ignorable="d" 
             x:Name="Root">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="130"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding ComponentLabel, ElementName=Root}" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ComponentText, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="24" Margin="0,0,0,5" VerticalContentAlignment="Center"/>
        <UIControls:WpfComponentCatalog Grid.Row="0" Grid.Column="2" x:Name="ComponentCatalog" Width="28" Height="24" Margin="2,0,0,5" SelectClicked="ComponentCatalog_SelectClicked" SelectionDone="ComponentCatalog_SelectionDone"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding PresetLabel, ElementName=Root}" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PresetText, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="24" Margin="0" VerticalContentAlignment="Center"/>
        <Button Grid.Row="1" Grid.Column="2" Content="..." Margin="5,0,0,0" Click="PresetButton_Click" Height="18" VerticalAlignment="Center" HorizontalAlignment="Left" Width="18"/>
    </Grid>
</UserControl>