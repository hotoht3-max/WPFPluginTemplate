<UserControl x:Class="Apibim.Plugins.BuiltUpColumn.UIControls.PartAttributesRowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UIControls="clr-namespace:Tekla.Structures.Dialog.UIControls;assembly=Tekla.Structures.Dialog"
             xmlns:localUI="clr-namespace:Apibim.Plugins.BuiltUpColumn.UIControls"
             mc:Ignorable="d" 
             x:Name="Root">
    <Grid Margin="0,2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="145"/>
            <ColumnDefinition Width="165"/>
            <ColumnDefinition Width="135"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="145"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
            <CheckBox x:Name="MasterChk" Click="MasterChk_Click" Margin="0,0,5,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding RowLabel, ElementName=Root}" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkProfile" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding Profile, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="105" Height="24" VerticalContentAlignment="Center"/>
            <UIControls:WpfProfileCatalog x:Name="ProfileCatalog" Width="28" Height="24" Margin="2,0,0,0" SelectClicked="ProfileCatalog_SelectClicked" SelectionDone="ProfileCatalog_SelectionDone"/>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkMaterial" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding Material, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="75" Height="24" VerticalContentAlignment="Center"/>
            <UIControls:WpfMaterialCatalog x:Name="MaterialCatalog" Width="28" Height="24" Margin="2,0,0,0" SelectClicked="MaterialCatalog_SelectClicked" SelectionDone="MaterialCatalog_SelectionDone"/>
        </StackPanel>

        <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkPartPrefix" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding PartPrefix, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="40" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkPartStartNo" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding PartStartNo, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="40" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="5" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkAssemblyPrefix" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding AssemblyPrefix, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="40" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="6" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkAssemblyStartNo" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding AssemblyStartNo, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="40" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="7" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkPartName" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding PartName, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="110" Height="24" VerticalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="8" Orientation="Horizontal" Margin="0,0,5,0">
            <UIControls:WpfFilterCheckBox x:Name="ChkPartClass" VerticalAlignment="Center" Margin="0,0,3,0"/>
            <TextBox Text="{Binding PartClass, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="40" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Column="9" Orientation="Horizontal">
            <UIControls:WpfFilterCheckBox x:Name="ChkUda" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <localUI:UdaLauncherControl ButtonText="UDA" Margin="0"/>
        </StackPanel>
    </Grid>
</UserControl>