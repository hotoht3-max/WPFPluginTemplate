<UserControl x:Class="Apibim.Plugins.BuiltUpColumn.UIControls.PartAttributesRowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UIControls="clr-namespace:Tekla.Structures.Dialog.UIControls;assembly=Tekla.Structures.Dialog"
             mc:Ignorable="d" 
             x:Name="Root">
    <Grid Margin="0,2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding RowLabel, ElementName=Root}" VerticalAlignment="Center" FontWeight="SemiBold"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,5,0">
            <TextBox Text="{Binding Profile, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="105" Height="24" VerticalContentAlignment="Center"/>
            <UIControls:WpfProfileCatalog x:Name="ProfileCatalog" Width="28" Height="24" Margin="2,0,0,0"
                                          SelectClicked="ProfileCatalog_SelectClicked" SelectionDone="ProfileCatalog_SelectionDone"/>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,5,0">
            <TextBox Text="{Binding Material, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="85" Height="24" VerticalContentAlignment="Center"/>
            <UIControls:WpfMaterialCatalog x:Name="MaterialCatalog" Width="28" Height="24" Margin="2,0,0,0"
                                           SelectClicked="MaterialCatalog_SelectClicked" SelectionDone="MaterialCatalog_SelectionDone"/>
        </StackPanel>

        <TextBox Grid.Column="3" Text="{Binding PartPrefix, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        <TextBox Grid.Column="4" Text="{Binding PartStartNo, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        <TextBox Grid.Column="5" Text="{Binding AssemblyPrefix, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        <TextBox Grid.Column="6" Text="{Binding AssemblyStartNo, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        <TextBox Grid.Column="7" Text="{Binding PartClass, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Height="24" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
    </Grid>
</UserControl>